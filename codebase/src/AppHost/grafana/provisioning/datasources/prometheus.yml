apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    # For local Aspire development, Prometheus runs in a container accessible via host.docker.internal
    # For Docker Compose, this would be http://prometheus:9090
    url: http://host.docker.internal:9090
    access: proxy
    isDefault: true
    editable: true

