{{- if .Values.customerService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "customer-spending-dashboard.fullname" . }}-customer-service
  labels:
    {{- include "customer-spending-dashboard.labels" . | nindent 4 }}
    component: customer-service
spec:
  replicas: {{ .Values.customerService.replicaCount }}
  selector:
    matchLabels:
      {{- include "customer-spending-dashboard.selectorLabels" . | nindent 6 }}
      component: customer-service
  template:
    metadata:
      labels:
        {{- include "customer-spending-dashboard.selectorLabels" . | nindent 8 }}
        component: customer-service
    spec:
      containers:
      - name: customer-service
        image: "{{ .Values.global.imageRegistry }}{{ .Values.customerService.image.repository }}:{{ .Values.customerService.image.tag }}"
        imagePullPolicy: {{ .Values.customerService.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.customerService.env.ASPNETCORE_ENVIRONMENT | quote }}
        - name: ASPNETCORE_URLS
          value: {{ .Values.customerService.env.ASPNETCORE_URLS | quote }}
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            configMapKeyRef:
              name: {{ include "customer-spending-dashboard.fullname" . }}-config
              key: Database__ConnectionString
        livenessProbe:
          httpGet:
            path: /graphql
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /graphql
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.customerService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-spending-dashboard.fullname" . }}-customer-service
  labels:
    {{- include "customer-spending-dashboard.labels" . | nindent 4 }}
    component: customer-service
spec:
  type: {{ .Values.customerService.service.type }}
  ports:
  - port: {{ .Values.customerService.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "customer-spending-dashboard.selectorLabels" . | nindent 4 }}
    component: customer-service
{{- end }}

