{{- if .Values.transactionService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "customer-spending-dashboard.fullname" . }}-transaction-service
  labels:
    {{- include "customer-spending-dashboard.labels" . | nindent 4 }}
    component: transaction-service
spec:
  replicas: {{ .Values.transactionService.replicaCount }}
  selector:
    matchLabels:
      {{- include "customer-spending-dashboard.selectorLabels" . | nindent 6 }}
      component: transaction-service
  template:
    metadata:
      labels:
        {{- include "customer-spending-dashboard.selectorLabels" . | nindent 8 }}
        component: transaction-service
    spec:
      containers:
      - name: transaction-service
        image: "{{ .Values.global.imageRegistry }}{{ .Values.transactionService.image.repository }}:{{ .Values.transactionService.image.tag }}"
        imagePullPolicy: {{ .Values.transactionService.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: {{ .Values.transactionService.env.ASPNETCORE_ENVIRONMENT | quote }}
        - name: ASPNETCORE_URLS
          value: {{ .Values.transactionService.env.ASPNETCORE_URLS | quote }}
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            configMapKeyRef:
              name: {{ include "customer-spending-dashboard.fullname" . }}-config
              key: Database__ConnectionString
        livenessProbe:
          httpGet:
            path: /graphql
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /graphql
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.transactionService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-spending-dashboard.fullname" . }}-transaction-service
  labels:
    {{- include "customer-spending-dashboard.labels" . | nindent 4 }}
    component: transaction-service
spec:
  type: {{ .Values.transactionService.service.type }}
  ports:
  - port: {{ .Values.transactionService.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "customer-spending-dashboard.selectorLabels" . | nindent 4 }}
    component: transaction-service
{{- end }}

